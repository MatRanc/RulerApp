name: Release
on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    defaults:
      run:
        working-directory: .
    steps:
      - uses: actions/checkout@v4
      - run: brew install xcodegen && xcodegen generate
      - name: Build (unsigned)
        run: |
          xcodebuild build \
            -project RulerApp.xcodeproj -scheme RulerApp \
            -configuration Release -destination 'generic/platform=macOS' \
            CONFIGURATION_BUILD_DIR="$PWD/build/Release" \
            CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=""
      - name: Make DMG
        run: |
          mkdir -p build/dmg
          cp -R build/Release/*.app build/dmg/
          ln -s /Applications build/dmg/Applications
          hdiutil create -volname RulerApp -srcfolder build/dmg -ov -format UDZO build/RulerApp.dmg
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || format('build-{0}', github.run_number) }}
          files: ./build/RulerApp.dmg
      - uses: actions/upload-artifact@v4
        with:
          name: RulerApp-dmg
          path: ./build/RulerApp.dmg
